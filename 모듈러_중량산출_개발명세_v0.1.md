# 모듈러 건축 중량 산출 웹앱 개발 명세 v0.1

작성일: 2026-09-08 · 상태: 구현 기준 초안 · 대상: 모델러, SketchUp Ruby 개발자, Claude 웹앱 개발자

## 0. 제품 결정과 범위

SketchUp Ruby Exporter → UTF-8 JSON → 웹 Weight Calculator 구조로 구현한다. 사용자는 SketchUp에서 부재의 형상·실제 위치·자재 Tag를 준비하고, 웹에서 자재 단중을 관리한다. 길이·면적·체적·중량을 사람이 반복 입력하지 않는다. 단, 형상만으로 산출 기준이 모호한 부재는 Exporter 속성 패널에서 기준면·기준축을 지정한다.

화면의 ‘중량’은 질량 kg를 의미한다. ton은 kg/1,000이다. 힘 kN, 양중 안전 판정, 크레인 선정, 지지점 반력 계산은 v0.1 범위 밖이다. CG 결과는 모델에 포함한 질량과 위치에 대한 결과이며 양중 승인 판정으로 표시하지 않는다.

### MVP 포함

- 프로젝트/산출 버전 관리, JSON 업로드와 검증, Tag 매핑, 자재 DB
- AREA/VOLUME/LENGTH/COUNT 계산, 부재·자재·공종·모듈 집계
- 수동 추가 중량, 산출 제외 사유, 무게중심(CG), 오류 처리
- 결과 고정, CSV 및 계산 근거 JSON 다운로드, 인쇄용 보고서
- 모듈 경계상자와 CG의 평면·정면·측면 표시. 정밀 3D 메시 뷰어는 후속 범위

### 구현 기본안

한국어 데스크톱 웹 우선. React + TypeScript, 계산 엔진은 UI와 분리한 순수 함수로 작성한다. 초기 단일 사용자 버전은 IndexedDB에 저장하고 프로젝트 백업 JSON을 제공한다. 서버·로그인·공동 편집은 추후 확장한다. 저장 실패 시 ‘저장됨’을 표시하지 않는다. 서버 도입 시에도 아래 데이터 계약과 계산 규칙을 유지한다.

## 1. SketchUp 사용자 작업 규칙

### 1.1 사용자가 완료해야 하는 범위

| 작업 | 필수 여부 | 완료 기준 |
|---|---|---|
| 실제 설치 부재 모델링 | 필수 | 주요 구조·바닥·벽·천장·창호·설비를 실제 크기와 위치로 배치 |
| 모듈 지정 | 필수 | 모듈 최상위 Group/Component를 선택하여 Exporter의 ‘모듈로 지정’ 실행 |
| 부재 Group/Component화 | 필수 | 하나의 산출 부재는 하나의 자재와 계산 기준을 가짐 |
| 자재 Tag 지정 | 필수 | 산출 부재 인스턴스에 DB와 일치하는 Tag 부여 |
| 공종 분류 | 필수 | 분류 그룹 또는 부재 속성에서 STRUCTURE/FLOOR/WALL/CEILING/OPENING/MEP/OTHER 선택 |
| 면·길이 기준 지정 | 조건부 | 등록 템플릿으로 판별 불가한 AREA/LENGTH 부재에만 지정 |
| 단중·중량·물량 숫자 입력 | 불필요 | 물량은 Exporter, 단중은 웹 자재 DB 담당 |
| 비균질 제품의 중심 지정 | 조건부 | 제조사 CG 정보가 있으면 기준점 지정, 없으면 추정임을 표시 |
| 미모델링 부재 보완 | 조건부 | 웹 수동 중량에서 위치와 근거 입력 |

볼트·나사·접착제 등을 전부 상세 모델링할 필요는 없다. 생략한 실제 설치 중량은 웹에서 별도 항목으로 추가한다. 구매 물량의 할증률을 설치 중량에 자동 곱하지 않는다.

### 1.2 권장 계층

```text
MODULE_A01                    role=MODULE, module_id는 자동 부여
  STRUCTURE                   role=CONTAINER, category=STRUCTURE
    Column_01                 role=PART, Tag=ST_HSS_100X100X4.5
    Beam_01                   role=PART, Tag=ST_HSS_150X100X4.5
  FLOOR                       role=CONTAINER, category=FLOOR
    FloorPlate                role=PART, Tag=ST_PLATE_6
    CementBoard               role=PART, Tag=BD_CEMENT_18
  WALL                        role=CONTAINER, category=WALL
    Gypsum_Inner               role=PART, Tag=BD_GYPSUM_12.5
    Insulation                role=PART, Tag=INS_GW_100
```

MODULE/CONTAINER는 집계용이며 자체 중량을 갖지 않는다. PART 아래 다른 PART가 있으면 오류로 처리하고 사용자가 구조를 정리한다. 숨김 여부와 관계없이 중복 부모·자식 중량을 합산하지 않는다. 모듈 밖 PART와 중첩 MODULE은 오류다. 일반 보조 형상은 role=IGNORE와 사유를 지정한다.

Tag는 PART Group/Component 인스턴스에만 지정한다. 원시 Face/Edge는 Untagged로 유지한다. 모듈이나 상위 그룹의 자재 Tag를 자식에게 상속하지 않는다. 공종만 가장 가까운 상위 CONTAINER에서 상속하며 부재의 명시값이 우선한다. 미지정 공종은 OTHER로 집계하되 경고한다.

Tag는 ASCII 대문자 영문·숫자·밑줄·마침표로 제한한다: `^[A-Z][A-Z0-9_.]*$`. 규격/두께를 이름에 넣되 이름을 파싱하여 단중이나 두께를 자동 확정하지 않는다. 앞뒤 공백과 대소문자는 자동 수정하지 않고 수정 제안을 표시한다. 서로 다른 Tag의 같은 자재 매핑은 허용하되 하나의 Tag는 한 산출 버전에서 하나의 자재 버전에만 연결한다.

### 1.3 계산유형별 모델링

| 유형 | 권장 형상 | 사용자의 추가 지정 | 금지 또는 주의 |
|---|---|---|---|
| AREA | 한쪽 기준면 또는 일정 두께 판재 | 템플릿 외 형상은 한쪽 기준면들을 선택하여 ‘면적 기준면’ 저장 | 앞뒤·옆면 총합이나 전체 표면적/2 사용 금지 |
| VOLUME | 닫힌 솔리드, 실제 공제 공간 반영 | 보통 없음 | 열린 면, 겹친 솔리드 합산, 단열재를 외피 면으로만 표현 금지 |
| LENGTH | 일정 단면의 직선 부재, 템플릿 로컬 X가 길이 방향 | 템플릿 외 부재는 길이 양 끝점 지정 | 월드 경계상자의 가장 긴 변을 길이로 사용 금지 |
| COUNT | 제품 하나당 인스턴스 하나 | 필요 시 제품 CG 점 | 여러 제품을 한 개체로 묶고 개수 추정 금지 |

AREA 기준면은 실제 구멍이 있는 면으로 만든다. 창문을 벽 위에 얹기만 한 경우 자동 공제하지 않는다. 같은 벽 양쪽 보드, 두 겹 보드는 실제 위치에 별도 PART로 만든다. 두께 판재의 기준면 중심은 중량 중심면과 다르므로 템플릿이 두께 방향/값을 보유하거나, 사용자가 중심면을 지정해야 한다. 기준면만 있는 경우 그 평면을 질량 위치로 사용하는 근사임을 표시한다.

VOLUME은 한 PART 안에 한 개의 닫힌 경계 솔리드를 원칙으로 한다. 중공 강관의 VOLUME 산출은 실제 속 빈 형상을 반영해야 한다. 강관은 LENGTH × 제품 단중을 기본으로 권장한다. 단면이 변하거나 곡선인 강재는 MVP LENGTH 자동 산출 대상에서 제외하고 분할 또는 VOLUME으로 작성한다.

LENGTH 템플릿의 끝점은 로컬 좌표로 저장한다. 회전·이동과 길이 방향 스케일은 변환 후 길이에 반영한다. 단면 방향 스케일이 바뀌면 기존 kg/m와 규격이 불일치하므로 오류로 처리한다. COUNT 제품은 크기 스케일 변경 시 단중 적합성 재검토 오류를 낸다. 모듈 자체는 이동·회전만 허용하고 반사·스케일·전단을 금지한다. PART 반사는 정상 변환으로 처리하되 중심점/면 방향 처리를 검증한다.

### 1.4 Exporter 속성 패널

AttributeDictionary 이름은 `modular_weight`, 내부 속성은 아래 계약을 따른다. 모델러는 패널로 설정하며 Ruby 콘솔 입력은 요구하지 않는다.

| 속성 | 대상 | 입력 방식/기본값 |
|---|---|---|
| role | 모든 그룹 | MODULE/CONTAINER/PART/IGNORE, 최초 등록 시 선택 |
| module_id | MODULE 인스턴스 | UUID 자동, 복사 시 충돌 검사 후 새 ID 부여 |
| category | CONTAINER/PART | 선택 목록, PART는 상속 가능 |
| quantity_basis | PART | REFERENCE_FACES/SOLID/AXIS_ENDPOINTS/INSTANCE, 템플릿 기본값 |
| reference_face_ids | AREA 정의 | 기준면 선택 도구, 편집 후 삭제된 면 참조는 오류 |
| axis_endpoints_local | LENGTH 정의 | 두 점 지정 도구 또는 템플릿 |
| centroid_override_local | PART 인스턴스 | 선택값, 근거 설명 필수 |
| exclude_reason | IGNORE 또는 제외 PART | 필수 문자열 |
| template_id/version | PART 정의 | 템플릿 적용 시 자동 |

공유 Component 정의의 기준면·기준축을 변경하면 모든 인스턴스에 영향을 준다. Exporter는 영향 인스턴스 수를 표시한다. 개별 형상 변경은 ‘고유하게 만들기’ 후 수행한다. 인스턴스 역할·공종·제외·CG 보정은 인스턴스 속성으로 보관한다.

### 1.5 내보내기 절차와 완료 기준

1. 모듈 지정, 부재 Tag/공종 확인.
2. ‘중량 데이터 검증’ 실행: 오류 행 클릭 시 SketchUp 해당 부재 선택·확대.
3. 기준면/기준축 및 열린 솔리드 오류 수정. 불가 항목은 검토 목록에 남긴다.
4. ‘JSON 내보내기’: 기본 범위는 모델 전체 등록 모듈. 숨긴 부재와 꺼진 Tag도 포함한다. 가시성은 기록만 한다. 제외는 명시적 설정으로만 한다.
5. 결과 창에 모듈 수, 포함/제외/오류 부재 수, 파일명을 표시한다. 오류 포함 JSON은 내보낼 수 있으나 ‘검토 필요’로 기록한다.

원본 SKP를 자동 수정하여 오류를 숨기지 않는다. ID 생성·속성 저장은 SketchUp Undo 가능한 한 작업으로 처리한다. 선택 범위만 내보내기는 MVP 미지원으로 전체/부분 모델 혼동을 방지한다.

## 2. 수량과 무게중심 계산 계약

### 2.1 책임 분리

Exporter는 형상에서 물량과 해당 물량의 중심을 SI 단위로 산출한다. 웹 자재 DB는 계산유형과 단중을 결정한다. Exporter는 `quantity_basis`에 맞는 metric을 제공하며 다른 metric을 임의로 채우지 않는다. 매핑 자재가 다른 metric을 요구하면 ‘모델 재산출 필요’ 오류로 표시한다. 원시 메시가 없는 웹에서 AREA를 VOLUME으로 변환하거나 형상 중심을 재계산하지 않는다.

### 2.2 형상 변환 및 물량

월드 변환 T는 루트부터 PART까지 인스턴스 변환을 순서대로 곱한 값이다. 모든 좌표는 SketchUp 월드 좌표계에서 m로 출력한다. 내부 inch는 길이 ×0.0254, 면적 ×0.0254², 체적 ×0.0254³로 변환한다. 단위 설정 화면이 mm이어도 변환 기준은 같으며 두 번 변환하지 않는다.

- AREA: 선택한 기준면의 변환 후 면적 합. 비균등 스케일에 단일 길이 배율 제곱을 사용하지 않는다. 중심은 구멍을 제외한 삼각형들의 면적 가중 중심이다. 일정 두께 판재는 중심면으로 이동한 좌표를 사용한다.
- VOLUME: 닫힘과 방향을 검증한 경계 메시를 월드로 변환하고 signed tetrahedra 적분으로 체적 및 체적 중심을 함께 계산한다. 기준점 r을 수치적으로 가까운 위치에 잡고, 각 삼각형 a,b,c에 대해 v=dot(a-r,cross(b-r,c-r))/6, tetra 중심=(r+a+b+c)/4. signed 합으로 중심을 구하고 전체 방향 반전만 정규화한다. 개별 tetra 절댓값 합은 금지한다. 구멍/내부 셸의 방향을 검증할 수 없으면 실패 처리한다. 이미 인스턴스 변환된 체적에 determinant를 다시 곱하지 않는다.
- LENGTH: 로컬 양 끝점을 T로 변환한 후 두 점 거리. 중심은 두 점 중점이며 균일 선밀도를 가정한다.
- COUNT: PART 인스턴스마다 1 EA. 공유 정의를 한 번만 세지 않는다. 중심은 제조사/사용자 지정점 우선, 없으면 월드 경계상자 중심을 ESTIMATED로 사용한다.

경계상자 중심은 일반 형상의 정확한 질량 중심이 아니다. AREA/VOLUME/LENGTH의 정상 산출에 경계상자 중심을 대체 사용하지 않는다. 균질 면밀도·체적밀도·선밀도 가정은 보고서에 기록한다.

### 2.3 중량 및 집계

```text
AREA:   m_i = area_m2 × unit_weight_kg_per_m2
VOLUME: m_i = volume_m3 × density_kg_per_m3
LENGTH: m_i = length_m × unit_weight_kg_per_m
COUNT:  m_i = count × unit_weight_kg_per_ea
M = Σ 포함되고 유효한 m_i + Σ 수동 추가 중량
C_world = Σ(m_i × c_i_world) / M
C_module = R_moduleᵀ × (C_world - origin_module_world)
```

철판을 AREA로 계산할 경우 자재 DB에서 kg/m² = 밀도 × 두께(m)를 계산해 단중으로 저장한다. 예: 테스트용 7,850 × 0.006 = 47.1 kg/m². 같은 항목에 두께를 다시 곱하지 않는다. 태그의 `6`을 자동으로 두께로 해석하지 않는다.

단중·물량·수동 중량은 유한 양수, COUNT는 양의 정수여야 한다. 제외 상태로 0중량을 표현하지 않는다. 계산 내부는 반올림하지 않고 화면에서 kg 2자리, ton 3자리, 물량 3자리, CG mm 1자리로 표시한다. 반올림한 행 합과 총계가 다를 수 있음을 보고서 주석에 기록한다. 정렬은 원값 기준이다.

집계의 기준 키는 모듈, 공종, 자재 버전, 계산유형이다. 서로 다른 단위의 물량은 합치지 않는다. 공종별/자재별 비율 분모는 현재 산출 범위의 유효 중량 합이다. 미해결 부재는 0kg로 표시하지 않고 ‘미산출’로 표시한다.

### 2.4 결과 상태

| 상태 | 조건 | 표시/허용 |
|---|---|---|
| EMPTY | 포함 부재 없음 | 총중량/CG ‘—’, 확정 불가 |
| BLOCKED | 파일/계약 오류 | 업로드 반영 안 함, 기존 버전 유지 |
| INCOMPLETE | 미매핑·물량 오류·단중 누락 | ‘산출 가능 항목 소계’, 전체 총중량·전체 CG 확정 금지 |
| CG_INCOMPLETE | 중량은 모두 유효, 일부 중심 없음 | 총중량 표시, 전체 CG ‘계산 불가’ |
| ESTIMATED | 중심 또는 단중에 추정 근거 포함 | ‘추정’ 배지, 근거와 확인 기록 후 추정 결과로 확정 가능 |
| VALID | 포함 항목 중량·중심 모두 유효 | 총중량·CG 표시, 확정 가능 |

여러 문제가 있으면 BLOCKED → EMPTY → INCOMPLETE → CG_INCOMPLETE → ESTIMATED → VALID 순서로 가장 제한적인 상태를 사용한다. INCOMPLETE에서 참고용 CG를 제공할 때는 ‘유효 항목만의 CG’로 분리하고 포함 중량과 부재 수를 함께 표시한다. 누락 항목 중량을 모르므로 ‘중량 기준 완성률’을 만들지 않는다.

수동 추가 중량은 양수 kg, 모듈, 공종, 설명, 출처가 필수이고 월드 위치는 선택 입력이다. 위치가 없으면 전체 CG가 CG_INCOMPLETE가 된다. 총중량 비율로 추정한 부대 중량은 우선 절대 kg로 기록하고 기준/비율을 근거에 남긴다. 기존 CG 위치에 자동 배치하지 않는다.

## 3. 웹앱 화면기획서

### 3.1 주요 사용자 흐름

프로젝트 생성 → JSON 업로드 → 검증/Tag 매핑 → 단중 검토 → 대시보드 → 부재·CG 검토 → 수동 중량 보완 → 결과 확정 → 보고서 다운로드.

수정 시 기존 확정 결과는 유지하고 새 Draft 산출 버전을 만든다. 형상은 SketchUp에서 수정 후 재업로드한다. 단중·매핑·추가 중량은 웹에서 수정한다. 재업로드는 덧붙이기 대신 원본 모델 스냅샷 전체 교체다.

### 3.2 전역 레이아웃

좌측 메뉴: 프로젝트 / 업로드·검증 / 중량 대시보드 / 부재 목록 / 무게중심 / 수동 중량 / 자재 DB / 보고서·이력.

상단: 프로젝트명, 산출 버전, Draft/확정 배지, 모듈 선택, 저장 상태. 본문 상단에 ‘미해결 N건’ 링크를 고정한다. 색은 보조로만 사용하고 오류·추정 텍스트와 아이콘을 병기한다. 기본 단위는 kg, CG는 mm. 단위 전환은 표시만 바꾼다.

대시보드는 총중량 카드, 결과 상태, CG 요약, 모듈/공종별 가로 막대, 자재 집계표 순서다. 최초 화면에 임의 샘플 실적을 표시하지 않는다. 데모 데이터는 명시적으로 ‘데모 불러오기’를 눌렀을 때만 표시한다.

1440px 기준 본문을 설계하되 1024px에서는 메뉴를 접고 표 가로 스크롤을 허용한다. 모든 입력에 라벨, 오류 설명, 키보드 포커스를 제공한다. 계산 중에는 이전 값을 최신 결과처럼 보여주지 않고 ‘재계산 중’으로 표시한다.

### 3.3 필터 규칙

상단 모듈 필터는 대시보드/부재/CG의 산출 범위를 통일한다. 부재 표의 검색·공종·상태 필터는 목록에만 적용하고 총중량을 바꾸지 않는다. 표 위에 ‘현재 표시 행 소계’를 별도로 제공한다. 보고서 생성 화면에서는 대상 모듈을 다시 명시한다. 필터 변경은 모델 포함/제외 변경이 아니다.

## 4. 화면별 화면정의서

### MW-01 프로젝트 목록·생성 — `/projects`

- 목적: 프로젝트와 최근 산출 접근.
- 구성요소: 검색, 프로젝트 카드/표, 새 프로젝트 모달, 백업 가져오기.
- 입력: 프로젝트명(1~100자), 설명(최대 1,000자). 출력: 최근 버전, 갱신일, 상태, 중량 또는 미산출.
- 인터랙션: 생성 후 MW-02 이동. 프로젝트 클릭은 마지막 버전 MW-05. 백업은 스키마 검증 후 새 프로젝트로 복원.
- 상태/오류: 빈 목록은 생성 안내; 저장 용량 초과는 저장 실패와 백업 안내; 같은 이름은 허용하되 ID는 고유.
- 계산 규칙: 카드 수치는 저장된 최신 버전 결과를 사용하며 확정값과 Draft를 구분한다.

### MW-02 JSON 업로드 — `/projects/:id/import`

- 목적: 모델 스냅샷을 안전하게 새 버전으로 반영.
- 구성요소: 파일 드롭존/선택, 파일명·모델명·Exporter 버전, 검증 진행, 변경 요약, 반영 버튼.
- 입력: `.json` 한 개, MVP 20MB/50,000 PART 제한. 출력: 모듈/부재/오류 수, 추가·삭제·변경 수.
- 인터랙션: 읽기 → 구문/스키마 검증 → 임시 저장 → 미리보기 → ‘새 버전으로 반영’. 취소는 임시 데이터만 폐기한다. 같은 파일 해시는 중복 안내하고 재반영하지 않는다.
- 상태/오류: 파싱 실패, 미지원 schema major, 잘못된 단위, 중복 ID, 끊어진 모듈 참조는 BLOCKED. 부재 측정 오류는 MW-03에서 해결 가능하도록 가져온다. 처리 중 재클릭 비활성.
- 계산 규칙: 업로드 단계에서는 중량을 확정하지 않는다. 동일 model_id+element_id로 변경 비교한다. 다른 model_id는 ID 연결을 시도하지 않고 별도 모델 교체 경고를 표시한다.

### MW-03 검증·Tag 매핑 — `/projects/:id/revisions/:rev/validation`

- 목적: 미등록 Tag, metric 불일치, 중심 누락 등 해결.
- 구성요소: 오류/경고 탭, Tag별 영향 부재 수, 자재 검색 드롭다운, 새 자재, 부재 링크, 일괄 매핑 검토창.
- 입력: Tag→material_version_id, 제외 사유, 경고 확인 메모. 출력: 필요한 계산유형·단위·단중, 영향 중량 미리보기, 해결 수.
- 인터랙션: 매핑 선택 → 영향 미리보기 → 적용 → 재계산. 형상 오류는 ‘SketchUp에서 수정 후 재업로드’와 instance_path를 제공한다. 웹에서 SketchUp 자동 선택은 연결 기능이 없으므로 약속하지 않는다.
- 상태/오류: 미매핑, 단위 불일치, metric 없음, 0/음수, 중복 산출, 중심 없음. 새 자재 생성 취소 시 기존 매핑 유지.
- 계산 규칙: 한 Tag의 전체 포함 부재에 같은 자재 버전 적용. 필요한 metric이 없는 항목은 미산출로 남긴다. 오류를 무시해 정상 상태로 바꾸는 버튼은 제공하지 않는다.

### MW-04 자재 DB·편집 — `/materials`

- 목적: 자재 규격과 단중의 출처·버전 관리.
- 구성요소: 검색/분류/유형 필터, 자재 표, 편집 패널, 출처 링크, 적용 프로젝트 영향 수.
- 입력: 이름, 규격, AREA/VOLUME/LENGTH/COUNT, 단중, 출처, 기준일, VERIFIED/ESTIMATED, 선택 두께·밀도. 출력: 단위 자동 표시, 버전, 유도 단중.
- 인터랙션: 저장은 새 자재 버전 생성. ‘이 Draft에 적용’은 영향 미리보기 후 실행. 과거 확정값에 자동 전파하지 않는다. 사용 중 자재는 삭제 대신 비활성화.
- 상태/오류: 빈 이름/출처, 유한 양수 아닌 값, 유형과 단위 불일치, 두께·밀도 미입력. ESTIMATED는 추정 사유 필수.
- 계산 규칙: AREA 직접 단중 또는 밀도×두께 중 하나의 입력 모드를 선택한다. COUNT는 kg/EA, 다른 단위 임의 입력 불가. 예시 DB 값은 모두 테스트용으로 표시한다.

### MW-05 중량 대시보드 — `/projects/:id/revisions/:rev/overview`

- 목적: 모듈 중량과 구성, 검토 상태 파악.
- 구성요소: 총중량/유효 소계, 포함·제외·미산출 수, CG 카드, 공종·모듈 차트, 자재 집계표, 확정 버튼.
- 입력: 모듈 범위, kg/ton 표시. 출력: 중량, 비율, 자재별 단위별 물량, 추정 배지.
- 인터랙션: 공종/자재 클릭은 MW-06 필터 이동, 오류 배지는 MW-03, CG 카드는 MW-08. 확정 전 버전·단중 출처·제외·추정 목록 확인.
- 상태/오류: 2.4 상태별 표시를 적용한다. 계산 실패는 실패 상태로 남기고 마지막 성공 결과에 시각적 ‘이전 결과’ 표시.
- 계산 규칙: 모델 항목과 수동 항목을 함께 합산한다. 필터 범위와 계산 버전을 카드에 표시한다. 미산출 존재 시 ‘총중량’이라는 완결 표현을 사용하지 않는다.

### MW-06 부재 목록 — `/projects/:id/revisions/:rev/elements`

- 목적: 부재별 산출 근거와 이상값 검토.
- 구성요소: 검색, 모듈/공종/Tag/상태 필터, 표, CSV, 상세 패널 링크.
- 입력: 검색 문자열, 정렬, 표시 열. 출력 열: 부재명, 모듈, 공종, Tag, 계산유형, 물량/단위, 단중/단위, 중량, 중심 품질, 상태.
- 인터랙션: 행 클릭 MW-07; 다중 선택 제외는 영향 중량·사유 검토 후 적용; 취소/복원 가능. 원본 물량은 읽기 전용.
- 상태/오류: 결과 없음, 대용량은 가상 스크롤, metric 없음은 ‘—/미산출’. 제외 부재는 별도 배지로 남긴다.
- 계산 규칙: 표 필터는 전체 산출값을 바꾸지 않는다. CSV는 ‘현재 표시 행’ 또는 ‘전체’ 범위를 명시하게 한다.

### MW-07 부재 상세 — MW-06 우측 패널

- 목적: 형상·단중·좌표·오류의 추적.
- 구성요소: element_id/instance_path 복사, 계층 경로, 자재 버전, 계산식, 수량 출처, 월드/모듈 중심, 경계상자 수치, 변경 이력.
- 입력: 포함/제외 사유, 검토 메모. 출력: 실제 대입 계산식, 중심 산출 방식과 가정, diagnostic 코드.
- 인터랙션: 자재 편집은 MW-04, 매핑은 MW-03. 사용자 CG 보정은 SketchUp 재내보내기를 기본으로 하며 웹에서 원본 중심을 덮어쓰지 않는다.
- 상태/오류: 이전 버전에서 삭제된 부재는 읽기 전용 기록. 잘못된 참조 ID는 상세 없음 안내.
- 계산 규칙: 원본 metric·적용 단중·엔진 버전으로 재현 가능한 산식 표시. 제외 부재의 잠재 중량은 ‘합계 제외’로 명시한다.

### MW-08 무게중심 — `/projects/:id/revisions/:rev/cg`

- 목적: CG 위치와 근거를 확인.
- 구성요소: 평면 XY/정면 XZ/측면 YZ, 모듈 경계상자, CG 십자, 좌표 카드, 추정/누락 목록, 좌표계 선택.
- 입력: 전체 또는 단일 모듈, 월드/모듈 좌표, 표시 mm/m. 출력: Cx/Cy/Cz, 질량, 기준 원점·축, 중심 품질.
- 인터랙션: 단일 모듈일 때만 모듈 좌표 활성. 추정 부재 클릭은 MW-07. 수동 항목 위치 누락은 MW-09로 연결.
- 상태/오류: M=0이면 나눗셈하지 않고 ‘—’. CG_INCOMPLETE는 전체 마커 숨김. INCOMPLETE의 부분 CG는 명시적 ‘참고 표시’ 선택 때만 별도 점선 마커 사용.
- 계산 규칙: 2.3 가중 중심식. 전체 프로젝트는 월드 좌표로만 계산한다. 모듈 로컬 표시는 역 강체 변환한다. 경계상자만으로 실제 부재 모양을 재현한 것처럼 표시하지 않는다.

### MW-09 수동 추가 중량 — `/projects/:id/revisions/:rev/manual`

- 목적: 모델링하지 않은 설치 중량 보완.
- 구성요소: 항목 표, 추가/편집 모달, 위치 입력, 출처·추정 표시.
- 입력: 이름, 모듈, 공종, kg, 월드 x/y/z(m, 세 값 모두 또는 모두 미입력), 출처, 사유. 출력: 추가 중량 합, 위치 누락 건수.
- 인터랙션: 저장 즉시 Draft 재계산. 삭제는 되돌리기 제공. 재업로드 시 유지하되 모듈이 사라지면 재할당 필요 상태.
- 상태/오류: 중량 ≤0, NaN, 좌표 일부 입력, 모듈 미지정. 같은 명칭은 중복 가능 경고만 제공한다.
- 계산 규칙: 각 행은 하나의 질량 집중점. 위치 누락 시 중량에는 포함하고 전체 CG는 불완전 처리한다. 제외 모델 부재를 보완했다면 참조 ID를 근거에 남긴다.

### MW-10 보고서·산출 이력 — `/projects/:id/revisions/:rev/reports`

- 목적: 재현 가능한 결과 전달과 버전 비교.
- 구성요소: 버전 목록, 대상 모듈 선택, 확정 상태, 요약/부재/근거/제외/추정 내역, CSV·근거 JSON·인쇄 버튼.
- 입력: 보고서 제목, 대상 모듈, 검토자 표시명, 비고. 출력: 날짜, 원본 해시, DB 스냅샷, 엔진 버전, 질량·CG·오류·추정/제외 목록.
- 인터랙션: Draft는 ‘검토용’ 워터마크. VALID 또는 확인 완료 ESTIMATED만 확정 가능. 확정본 수정은 복제 Draft 생성. 이력 비교는 같은 모델 occurrence ID로 추가·삭제·물량·단중·중량 차이를 표시한다.
- 상태/오류: INCOMPLETE/CG_INCOMPLETE는 검토용 출력만 가능. 인쇄 취소 시 상태 변경 없음. 다운로드 실패 시 재시도 가능.
- 계산 규칙: 화면 반올림 전 값을 원본 JSON에 보존. 비교의 기준 버전과 대상 버전을 표시하고 추정값 차이를 확정된 증감처럼 설명하지 않는다.

## 5. 데이터 모델과 버전 정책

| 엔터티 | 핵심 필드 |
|---|---|
| Project | id, name, description, created_at |
| ModelImport | id, project_id, model_id, export_id, raw_json, sha256, imported_at |
| Revision | id, project_id, import_id, parent_revision_id, status, calculation_engine_version |
| MaterialVersion | material_id, version_id, name, specification, calc_type, unit_weight, unit, source, source_date, quality, thickness_m?, density_kg_m3? |
| TagMapping | revision_id, tag, material_version_id |
| ElementOverride | revision_id, element_id, excluded, reason |
| ManualMass | id, revision_id, module_id, category, mass_kg, centroid_world_m?, source, reason |
| ResultSnapshot | revision_id, input_hash, material_snapshot, mapping_snapshot, overrides, manual_masses, totals, cg, diagnostics, fixed_at? |

업로드 원본과 확정 snapshot은 불변이다. Draft 수정은 저장 후 계산 입력 해시를 갱신한다. 계산 결과에는 입력 해시를 붙여 오래된 비동기 응답을 버린다. 자재 DB 최신값을 계산 중 동적으로 읽지 않고 해당 버전 스냅샷을 사용한다.

재업로드 시 같은 model_id의 동일 element_id에만 제외 메모를 승계한다. 매핑은 같은 Tag와 기존 자재 버전을 승계하되 metric 적합성을 다시 검사한다. 삭제 부재의 override는 새 산출에서 적용하지 않는다. 수동 항목은 모듈 참조가 유효한 경우만 유지 적용한다. 이전 경고 확인은 형상 또는 단중이 바뀌면 초기화한다.

## 6. SketchUp Export JSON 인터페이스 초안

### 6.1 파일 규약

파일명 권장: `{model_name}_{UTC timestamp}.weight.json`. UTF-8, 순수 JSON, 주석/NaN/Infinity 금지. 날짜는 ISO 8601 UTC. 숫자는 지수 표기를 허용하는 유한 JSON number. schema_version은 `1.0.0`; major 미지원은 반영 차단, 알려지지 않은 optional 필드는 경고 후 보존한다.

Export JSON에 단중과 최종 중량은 넣지 않는다. 첨부 `sample_export.json`은 네 계산유형을 보여주는 계약 예시이며 제품 단중 데이터가 아니다.

### 6.2 필드 정의

표에서 ?는 선택, nullable은 명시적 null 허용. 그 외 필드는 필수다.

| 경로 | 타입/규칙 | 의미 |
|---|---|---|
| schema_version | string | 계약 버전 |
| export_id | UUID string | 내보내기마다 새 ID |
| exported_at | UTC string | 내보낸 시간 |
| exporter | object: name, version, sketchup_version strings | 실행 환경 |
| model | object: id, name strings | id는 모델 속성 UUID, 이름 변경과 독립 |
| units | object, length=m, area=m2, volume=m3, count=EA | 고정 SI |
| coordinate_system | object: frame=SKETCHUP_WORLD, handedness=RIGHT, up_axis=Z | 월드 기준 |
| scope | object: mode=ALL_REGISTERED_MODULES, includes_hidden=true | 범위 계약 |
| modules | Module[] | ID 중복 불가 |
| elements | Element[] | 인스턴스 occurrence별 한 행 |
| diagnostics | Diagnostic[] | 파일/모듈/부재 오류 |

Module: `id:string`, `name:string`, `origin_world_m:Vec3`, `axes_world:{x:Vec3,y:Vec3,z:Vec3}`, `bounds_world_m:{min:Vec3,max:Vec3}`. 축은 우수 직교 단위벡터이며 허용오차 1e-6. origin과 축은 모듈 로컬 좌표를 월드로 옮기는 강체 프레임이다. bounds는 월드 축 정렬 경계상자다.

Element:

| 필드 | 타입/규칙 |
|---|---|
| id | string, 파일 내 고유; 권장 model UUID + ':' + persistent_id_path |
| instance_path | string, SketchUp persistent ID path |
| name | string |
| module_id | string, 존재하는 Module 참조 |
| category | STRUCTURE/FLOOR/WALL/CEILING/OPENING/MEP/OTHER |
| tag | string 또는 null; null은 미매핑 오류로 가져오기 허용 |
| quantity_basis | REFERENCE_FACES/SOLID/AXIS_ENDPOINTS/INSTANCE |
| included | boolean |
| exclude_reason | string 또는 null; included=false이면 비어 있으면 안 됨 |
| visibility | object: hidden:boolean, tag_visible:boolean; 계산 포함 여부와 독립 |
| metrics | object: area_m2, volume_m3, length_m, count; 각각 Metric 또는 null |
| bounds_world_m | object: min/max Vec3 |

Vec3는 정확히 3개의 유한 number 배열이다. Metric은 `value:number`, `centroid_world_m:Vec3|null`, `quantity_method:string`, `centroid_method:string`, `quality:CALCULATED|USER_SPECIFIED|ESTIMATED`, `assumptions:string[]`로 구성한다. quality는 주로 중심 근거 수준을 나타내며 물량 방법은 quantity_method로 별도 기록한다. USER_SPECIFIED는 assumptions에 출처가 필수다. metric.value는 양수, count.value는 정수다. 산출 실패한 metric은 null로 넣고 diagnostic을 추가한다. 웹은 진단이 누락되더라도 필요한 metric의 null을 직접 검출한다.

quantity_method 허용값: TRANSFORMED_REFERENCE_FACES / CLOSED_MESH_INTEGRAL / TRANSFORMED_AXIS_ENDPOINTS / INSTANCE_ONE.

centroid_method 허용값: AREA_WEIGHTED / VOLUME_INTEGRAL / AXIS_MIDPOINT / USER_POINT / BBOX_CENTER / UNAVAILABLE. UNAVAILABLE일 때만 centroid_world_m=null이다. BBOX_CENTER이면 quality=ESTIMATED를 강제한다.

Diagnostic: `code:string`, `severity:ERROR|WARNING|INFO`, `element_id:string|null`, `module_id:string|null`, `message:string`. 코드로 분기하고 번역 가능한 message를 계산 로직에 사용하지 않는다.

### 6.3 ID/변환/호환성 주의

같은 Component 정의가 20번 배치되면 20개 element다. leaf persistent_id만으로 식별하면 반복 인스턴스가 충돌하므로 전체 instance_path를 쓴다. 부모 변경·폭파·재생성은 ID가 달라질 수 있으며 자동 동일 부재로 추측하지 않는다. 모델 Save As는 모델 UUID가 유지될 수 있으므로 Exporter에 ‘새 모델 ID 발급’을 제공하고 웹은 model_id와 파일 해시를 모두 표시한다.

모든 metrics와 중심은 이미 월드 변환·SI 변환을 마쳤다. 웹에서 다시 스케일하거나 인스턴스 변환을 곱하지 않는다. v1은 raw mesh와 행렬을 전달하지 않는다. 정확한 3D 뷰어와 웹 형상 재산출은 차기 계약이다.

### 6.4 진단 코드

| 코드 | 수준 | 해결 위치 |
|---|---|---|
| INVALID_SCHEMA / DUPLICATE_ID / INVALID_MODULE_REF / INVALID_FRAME | 파일 차단 | Exporter 수정/재출력 |
| MISSING_TAG / UNMAPPED_TAG | 부재 ERROR | SketchUp Tag 또는 웹 매핑 |
| MISSING_METRIC / BASIS_MISMATCH | 부재 ERROR | 기준 지정 후 재출력 |
| NON_MANIFOLD / INVALID_REFERENCE / DEGENERATE_GEOMETRY | 부재 ERROR | SketchUp 형상 수정 |
| NESTED_PART / OUTSIDE_MODULE / INVALID_SECTION_SCALE | 부재 ERROR | SketchUp 구조·스케일 수정 |
| INVALID_UNIT_WEIGHT | 부재 ERROR | 웹 자재 DB |
| MISSING_CENTROID | CG ERROR | SketchUp 기준점 또는 수동 위치 |
| ESTIMATED_CENTROID / ESTIMATED_UNIT_WEIGHT / UNCLASSIFIED | WARNING | 근거 검토/분류 수정 |

## 7. 개발 순서와 검수 기준

1. JSON 타입/검증기, 계산 엔진, 고정 fixture를 먼저 작성한다.
2. MW-01~04 구현으로 업로드·검증·매핑을 연결한다.
3. MW-05~09를 동일 계산 결과 selector에 연결한다. 화면마다 별도 합산식을 만들지 않는다.
4. 버전 고정, 보고서·백업 복원, 재업로드 차이 비교를 구현한다.
5. SketchUp Exporter를 연결하고 아래 실제 모델 검수를 수행한다.

### 필수 수용 테스트

| 번호 | 입력/상황 | 기대 결과 |
|---|---|---|
| AT-01 | LENGTH 2m, 단중 10kg/m, 중심 (1,0,0) | 20kg |
| AT-02 | AREA 6m², 5kg/m², 중심 (0,1,0) | 30kg |
| AT-03 | VOLUME 0.1m³, 1,000kg/m³, 중심 (0,0,1) | 100kg |
| AT-04 | COUNT 1, 50kg/EA, 중심 (2,0,0) | 50kg |
| AT-05 | 위 네 행 합계 | 200kg, CG=(0.6,0.15,0.5)m |
| AT-06 | 2×3m 두께 판재 AREA | 앞뒤·옆면 합이 아닌 6m² |
| AT-07 | 위 기준면 1m² 구멍 | 5m², 구멍 제외 중심 이동 반영 |
| AT-08 | 동일 정의 4개 배치, 각 20kg | 80kg, 다른 instance_path 4개 |
| AT-09 | 2m 직선 부재를 회전 | 길이는 2m 유지, 중심은 회전에 따라 이동 |
| AT-10 | 단면 방향 2배 스케일한 LENGTH 강관 | INVALID_SECTION_SCALE, 단중 자동 적용 금지 |
| AT-11 | 열린 VOLUME 형상 | 미산출, 0kg 정상값 표시 금지 |
| AT-12 | 한 Tag 미등록 | 유효 소계만 표시, 확정 차단 |
| AT-13 | 10kg 수동 항목, 위치 없음 | 총 210kg, 전체 CG 계산 불가 |
| AT-14 | 포함 항목 없음 | 총중량·CG ‘—’, NaN 없음 |
| AT-15 | Tag 꺼짐/부재 숨김 | 포함 수와 중량 동일 |
| AT-16 | 확정 이후 DB 단중 변경 | 기존 확정 결과 불변, 새 Draft에서만 반영 |
| AT-17 | 같은 export 재업로드 | 중복 안내, 중량 두 배로 늘지 않음 |
| AT-18 | 모델/모듈 이동·90도 회전 | 월드 CG 변환, 모듈 로컬 CG 동일 |
| AT-19 | 파일의 잘못된 모듈 참조/중복 ID | 원자적으로 업로드 차단, 기존 데이터 보존 |
| AT-20 | AREA 비균등 스케일과 PART 반사 | 변환 후 실제 면적·중심 일치, 음수 물량 없음 |
| AT-21 | 중심이 bbox 중심과 다른 L자 솔리드 | 체적 적분 중심과 일치 |
| AT-22 | 부모 PART와 자식 PART | 오류 표시, 두 번 합산하지 않음 |

순수 계산 테스트는 절대오차 1e-9, 실제 기하 Exporter fixture는 길이 1e-6m·면적 1e-6m²·체적 1e-9m³ 또는 상대오차 1e-6 중 큰 허용값으로 비교한다. 퇴화 형상 판정 허용오차는 모델 크기에 비례하도록 별도 구현한다. 20MB/50,000부재는 지원 한도 목표이며 성능을 검증한 사실이 아니다. 해당 fixture로 UI 응답과 저장 한도를 실제 측정하고 출시 조건을 정한다.

## 8. Claude에게 전달할 구현 요청문

아래 내용과 이 명세 전체, sample_export.json을 함께 전달한다.

> 모듈러 건축 중량 산출 웹앱을 React와 TypeScript로 구현해줘. 첨부 명세 v0.1을 데이터·계산·화면 계약으로 사용해줘. SketchUp 파일을 브라우저에서 직접 파싱하지 않고 Export JSON을 받는 구조야. MW-01부터 MW-10까지 실제로 연결된 화면을 만들고, AREA/VOLUME/LENGTH/COUNT와 자재 버전별 Tag 매핑, 수동 추가 중량, 모듈별/전체 CG를 구현해줘. 먼저 타입·검증기·순수 계산 엔진과 AT-01~05 테스트를 작성한 뒤 화면에 연결해줘. 누락값을 0으로 처리하지 말고 INCOMPLETE/CG_INCOMPLETE/ESTIMATED 상태를 명세대로 표시해줘. IndexedDB 저장, 프로젝트 백업/복원, 원본 불변, 확정 스냅샷, CSV 및 인쇄용 보고서도 포함해줘. 샘플 단중은 테스트용이며 실무 기본값으로 취급하지 마. 완료 시 실행 방법, 검수 결과, 미구현 범위를 설명해줘. 정확한 3D 메시나 양중 안전 판정은 이번 범위에 추가하지 마.

## 9. API 근거와 설계 가정

SketchUp Face.area는 변환을 인자로 받아 상위 스케일이 반영된 면적을 구할 수 있다. 본 명세의 AREA 변환은 이 기능 또는 동등한 변환 삼각형 면적 계산을 사용한다. [공식 Face API](https://ruby.sketchup.com/Sketchup/Face.html)

InstancePath는 전체 경로의 변환과 persistent ID 경로를 제공한다. 반복 인스턴스 구분과 월드 변환의 근거다. [공식 InstancePath API](https://ruby.sketchup.com/Sketchup/InstancePath.html)

Group.volume은 manifold 솔리드에 대한 체적 API다. 본 명세는 체적뿐 아니라 중심도 필요하므로 닫힌 메시 적분을 구현 계약으로 채택했다. Ruby API에 일반 형상의 정확한 질량 중심을 자동 제공한다고 가정하지 않는다. [공식 Group API](https://ruby.sketchup.com/Sketchup/Group.html)

모델링 템플릿, 속성 패널, 기준면/축 선택, 메시 중심 적분, 웹 화면은 새로 개발할 기능이다. 이 문서는 실행된 Exporter나 검증된 구조계산 결과가 아니다. SketchUp Desktop Ruby 환경을 대상으로 하며 실제 지원 버전은 사용자의 설치 버전에서 통합 테스트 후 명시한다.

## 10. 함께 전달할 파일과 검증 범위

- `sample_export.json`: 네 계산유형을 포함한 Export JSON 예제.
- `sample_calculation_fixture.json`: 웹 테스트용 자재·Tag 매핑과 기대 결과. Exporter 입력 계약과 별개의 웹 계산 fixture다.
- 두 예제는 JSON 파싱을 확인했고 예제 물량×단중의 합계 200kg, CG=(0.6, 0.15, 0.5)m를 별도 산술 계산으로 확인했다. 자재 값이 가상이므로 결과 상태는 ESTIMATED다.
- 실제 Ruby Exporter 실행, SketchUp 형상 검수, 웹 화면 구현 및 성능 테스트는 이 문서 작성 범위에서 실행하지 않았다.
